[project]
name = "mlmorph-web"
version = "0.1.0"
description = "Web API for Malayalam morphology analyser"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.1",
    "gunicorn",
    "mlmorph",
    "pydantic>=2.6.4",
    "regex",
    "uvicorn>=0.29.0",
]

[tool.uv]
package = false
